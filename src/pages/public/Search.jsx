import React, { useEffect } from 'react'
import SearchBar from '../../components/common/SearchBar'
const Search = () => {
  
    const [showSearch,setShowSearch]=useEffect(false);

  return (
    <div>
      
    </div>
  )
}

export default Search
